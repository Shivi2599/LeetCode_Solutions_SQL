import pandas as pd

def find_products(products: pd.DataFrame) -> pd.DataFrame:
    recyclable_and_low_fat_products = products[(products['low_fats'] == 'Y') & (products['recyclable'] == 'Y')]
    return recyclable_and_low_fat_products[['product_id']]
