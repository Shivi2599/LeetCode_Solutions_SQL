import pandas as pd

def second_highest_salary(employee: pd.DataFrame) -> pd.DataFrame:
    unique_salaries = employee['salary'].drop_duplicates().sort_values(ascending = False).reset_index(drop = True)
    if len(unique_salaries) <2:
        return pd.DataFrame({f'SecondHighestSalary':[None]})
    return pd.DataFrame({f'SecondHighestSalary':[unique_salaries[1]]})
